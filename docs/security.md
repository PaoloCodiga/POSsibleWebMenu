# Security

The public route normalizes only allowlisted configuration. It has no caller-provided domain or ordering input and validates pricelists for the active website company.

The sole `innerHTML` assignment is the documented trusted-QWeb generated-content boundary. Editor feedback is static translated markup; backend exception details are never exposed. Cleanup targets only named runtime descendants in a detached clone and cannot remove unrelated authored content.
